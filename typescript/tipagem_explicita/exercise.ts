let nomeLoja = 'Pizzaria do bairro'
let endereco: string;

function calcularTotal (precoItem: number, taxaEntrega: number): number {
    let totalApagar: number;
    totalApagar = precoItem + taxaEntrega;

    
    return totalApagar
}

console.log(calcularTotal(40, 5));
